{% import "macros/forms.twig" as form %}

<form action="" method="POST">
	<div class="row">
		<div class="col s4">{{ form.input_inline('name') }}</div>
		<div class="col s6">{{ form.input_inline('value') }}</div>
		<div class="col s2">{{ form.button('Save') }}</div>
	</div>
</form>